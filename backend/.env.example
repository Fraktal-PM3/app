# Backend Server Configuration
PORT=4000

# Frontend CORS Configuration
FRONTEND_URL=http://localhost:3000

# Hyperledger Firefly Configuration
FIREFLY_WS_URL=ws://127.0.0.1:FFport/ws?namespace=default&ephemeral&autoack&name=app&filter.events=message
FIREFLY_HTTP_URL=http://127.0.0.1:FFport
FIREFLY_NAMESPACE=default
FIREFLY_RECONNECT_INTERVAL=5000
FIREFLY_MAX_RECONNECT_ATTEMPTS=10

# Database Configuration (for future use)
# DATABASE_URL=postgresql://user:password@localhost:5432/delivery_app
# DATABASE_TYPE=postgresql