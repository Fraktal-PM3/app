{
  "name": "fraktal-pm3-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "eslint": "^9.18.0"
  }
}